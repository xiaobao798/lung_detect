# 医保 AI 识图大赛赛道一：最低可用实施方案

## 1. 项目成功定义

本项目的第一目标不是模型精度，而是完整跑通以下闭环：

```text
测评容器自动启动
  -> GET /health 返回 200
  -> POST /call 在 5 秒内返回 200
  -> 后台读取请求中的 dataset_path
  -> 为本次 evaluation_id 生成完整、合法结果
  -> 原子写入 answer/{evaluation_id}
  -> 使用原 request_id 和 evaluation_id 回调
  -> 平台成功读取结果并形成一次有效成绩
```

只有这个闭环通过后，才投入时间提高预测质量。

最低成功标准分为两级：

- 测评容器能够无人值守启动；
- 健康检查持续通过；
- 同一请求重复到达时不会重复破坏结果；
- 所有测试检查都有 `prediction.json`；
- `duplicate_pairs.jsonl`、概率、枚举、UID、路径均合法；
- 回调失败可重试，且保留可排查日志；
- 至少成功完成一次官方验证测评和一次正式提交。

其中“有效提交”只要求工程闭环；“进入排名”还必须同时通过非人体、拼接、重复影像三项医保安全门槛。门槛值将在正式推理测试前约 48 小时公布，任一项未达标都不能参与初赛总分排名。因此，常量基线只能作为格式验收版本，不能作为最终可排名版本。

## 2. 必须先确认的官方事实

上下文文件中的字段和目录示例不能替代官方规范。编码前应从官方开发文档、样例包或验证测评确认：

1. `prediction.json` 的完整 JSON Schema：字段类型、必填项、空值规则、坐标顺序和概率对象结构；
2. `predPath` 应指向 `answer/{evaluation_id}`、其下的 `predicate`，还是某个结果文件；
3. callback 的真实服务地址、鉴权方式、超时、成功响应格式和重试要求；
4. `/call` 首次返回体是否有固定格式；
5. 测试集真实目录层级，以及 StudyUID、SeriesUID 从目录名还是 NIfTI 元数据取得；
6. 一个检查包含多序列时，`prediction.json` 放置位置及主序列选择规则；
7. `BoundingBox`、`KeyPoints`、热力图的坐标系：体素坐标还是物理坐标，轴顺序、方向矩阵和单位；
8. 阴性结果能否省略病灶级字段，`Lesions=[]` 是否完全合法；
9. `duplicate_pairs.jsonl` 在测试集只有一个检查时的合法写法，以及是否允许空文件；
10. 自建模型组对“必须训练”“预训练来源”“训练日志”和模型权重的合规要求。

这些项目中任何一项不明确时，应以官方样例和一次最小验证测评作为格式判定器。

## 3. 推荐的最小系统架构

```text
lung_minimal/
├── server.py              # /health、/call、后台任务调度
├── config.py              # 根目录、callback、超时等环境变量
├── worker.py              # 单次 evaluation 的编排
├── dataset.py             # 发现检查/序列，读取 UID 和 NIfTI 元信息
├── predictor.py           # 统一预测接口；先常量基线，后替换模型
├── duplicate.py           # 重复检查候选生成
├── schema.py              # 输出数据结构、枚举和严格校验
├── writer.py              # 临时目录写入、校验、原子提交
├── callback.py            # 回调和有限重试
├── logging_jsonl.py       # 真实训练/推理事件日志
├── validate_output.py     # 离线检查整棵输出目录
├── requirements.txt
├── start.sh               # 容器前台启动命令
└── tests/
    ├── fixtures/          # 人工构造的小型假数据，不含官方数据
    ├── test_api.py
    ├── test_schema.py
    └── test_end_to_end.py
```

设计约束：

- 依赖尽量只保留 Web 框架、PyTorch、NumPy 和 NIfTI 读取库；
- HTTP 层不加载整套数据，不执行长任务；
- 单 GPU 串行执行即可，避免同一容器内多个推理任务争抢显存；
- 所有根路径和 callback 地址通过环境变量配置，禁止写死测试路径；
- 输出先写入同一文件系统中的临时目录，全部校验通过后再原子重命名；
- 每个 evaluation 单独记录状态：`accepted/running/succeeded/failed/callback_succeeded`；
- 日志不得包含患者隐私或完整影像内容。

## 4. 分阶段实施

### 阶段 A：格式闭环，不训练模型

先实现一个合法的常量基线：

- 对每个真实检查生成阴性预测；
- `IsNotHumanBodyProb`、`IsStitchedProb`、`MalignancyScore` 使用合法范围内的保守常量；
- `HasMalignantLesion=false`、`LesionCount=0`、`Lesions=[]`；
- UID 必须从输入数据真实提取，不能编造；
- `ProcessingTime_ms` 记录真实耗时；
- `Interpretation` 使用固定、无诊断承诺的简短说明；
- 重复检查先用低成本指纹排序，且只输出测试集内 UID。

测试集中每一例影像，即使被预测为假人体或拼接影像，也必须输出完整的临床诊断结果；评分端使用金标准决定哪些病例参与临床得分。

重复影像指纹可依次采用：文件大小/体积形状、重采样后的低分辨率强度直方图、中心切片感知哈希。只对高相似候选计算分数，并限制每个检查 Top-200。这样无需训练，也比随机配对更合理。

阶段 A 的产物必须先通过本地模拟请求，再创建测评镜像做官方验证。

### 阶段 B：医保安全最低可排名版本

- 假人体：低分辨率 3D 或 2.5D 二分类模型；
- 拼接影像：覆盖全体积的多切片二分类模型；
- 重复影像：低分辨率全局 embedding 加近邻检索，每例输出 Top-K 候选并校准成对概率。

三项均需要产生有区分度的连续概率。因为医保安全得分采用三项调和平均且设置单项门槛，不能放弃其中任何一项。

### 阶段 C：盘点官方资产，选择唯一主路线

在普通容器中只做只读盘点：

```bash
find /2026aicompetition/public_models -maxdepth 3 -type f | sort
find /2026aicompetition/datasets/training -maxdepth 4 -type f | head -n 200
```

输出一份资产清单：模型框架、权重、许可证/允许用途、输入尺寸、推理入口、依赖，以及训练标注的样本数和类别分布。

路线选择：

1. 有官方肺结节检测权重：直接接入，优先完成病灶框、中心点和恶性概率；
2. 有官方 nnU-Net/分割权重：用连通域把分割转换为病灶框和中心点；
3. 只有可训练 baseline：严格按 baseline 训练一次小模型；
4. 都没有：保留阶段 A 的合法基线，不临时自研复杂 3D 检测器。

只选择一条主路线，不并行维护多个框架。

### 阶段 D：最低成本提升临床得分

提升顺序按“收益/工程风险”排序：

1. 接入官方结节检测或分割模型；
2. 由候选病灶的物理直径确定 `NoduleSize`；
3. 用候选区域平均 HU、方差等简单特征给出密度类别；
4. 用肺叶定位模型或官方字段映射生成位置；没有可靠工具时输出规范允许的未知/默认值；
5. 用小型 patch 分类头预测良恶性；
6. 病理、T 分期、伴随征象仅在有明确训练标签且能低成本接入时实现。

不要为了填满字段而伪造高置信度。规范允许省略的字段应按规范省略；规范要求必填的概率分布才使用经过验证的保守先验。

赛道一临床任务中，结节检测和良恶性占 40%，病理与 T 分期占 20%，密度、位置、大小、伴随征象占 40%。漏检结节的后续属性全部计 0，因此必须先提升结节检测，再做属性分类。

### 阶段 E：冻结与正式提交

- 固定镜像摘要、代码提交号、依赖版本和权重校验和；
- 使用全新测评容器进行冷启动测试；
- 模拟 callback 暂时失败、重复 `/call`、损坏输入、磁盘已有旧结果等情况；
- 检查所有日志来自真实运行；
- 正式提交前不再升级框架或 CUDA 相关依赖。

## 5. 关键工程细节

### `/call` 行为

接到请求后只做轻量工作：

1. 校验 `request_id`、`evaluation_id`、`dataset_path`；
2. 确认 `dataset_path` 存在且位于平台允许的数据根目录内；
3. 以 `request_id + evaluation_id` 做幂等键；
4. 将任务放入单消费者队列；
5. 立即返回 200 和 `accepted` 状态。

已成功的同一任务再次到达时，不应重算；可以重新执行尚未成功的 callback。正在运行的同一任务再次到达时，应返回已接受状态。

### 输出校验器

在 callback 前必须检查：

- 输出检查集合与输入检查集合一致；
- 文件名和目录层级正确；
- JSON/JSONL 均能逐个解析；
- 字段名、类型、枚举值严格合法；
- 所有概率有限且位于 `[0,1]`；
- 多分类概率和在容差内等于 1；
- `LesionCount == len(Lesions)`；
- 所有 UID 来自当前输入；
- 框、点和热力图落在对应影像物理范围内；
- 不存在 NaN、Infinity、NumPy 标量或不可序列化对象；
- 没有把临时文件、调试文件放入结果目录。

### 失败处理

- 推理失败：保存错误状态和简化堆栈，不发送“成功”回调；
- 单个病例失败：是否允许生成保守阴性结果，应以官方规则为准；
- callback 失败：指数退避、有限重试，并持久化待回调状态；
- 进程重启：启动时扫描未完成任务状态，但不得猜测已丢失的原请求参数；
- 输出已存在：只在校验通过且任务身份一致时复用。

## 6. 建议的里程碑与停止条件

### M0：资料齐全

拿到官方开发规范、完整输出样例、callback 说明、测试请求样例和自建模型合规规则。

### M1：本地闭环

人工小数据上，API、后台任务、结果生成、校验和模拟 callback 全部通过。

### M2：官方验证测评有效

阶段 A 常量基线在官方验证测评中形成有效成绩。达到后，项目已具备“可提交”状态。

### M3：医保安全门槛模型

三项医保安全任务均具备真实模型输出，并在验证集上具有非零、可排序的区分能力。

### M4：接入一个真实临床模型

只接入一个官方模型，确认至少一个核心指标不低于常量基线，且没有破坏稳定性。

### M5：正式提交

使用冻结镜像完成提交并保存提交编号、镜像摘要、代码提交号和结果日志。

停止条件：M2 完成后已经具备“格式有效提交”能力；若目标是进入排名，则 M3 是必要条件。临床模型是否继续优化可根据剩余时间决定。

## 7. 当前最合理的下一步

不要立即写模型。先在可信环境收集以下四样资料：

1. 官方输出样例和开发规范原文；
2. `public_models` 的两到三层文件清单；
3. `training` 的目录与标注样例（只在可信环境内查看）；
4. 平台提供的 callback 地址/配置方式和验证测评操作说明。

拿到这些信息后，先实现阶段 A，并把第一次官方验证测评当作最高优先级验收。
